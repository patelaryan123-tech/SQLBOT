import React from 'react';

/**
 * AdminRoute — Localhost Only version.
 * Always allows access in local development mode.
 */
const AdminRoute = ({ children }) => {
  return children;
};

export default AdminRoute;
